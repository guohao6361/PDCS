# 云原生微服务电商系统 — 后端实现文档

## 一、概述

### 1.1 系统架构

```
                    用户浏览器
                        |
                   Nginx(:80)
                  /  |  |  \  \
    静态资源  /users /cart /products /orders
                |      |      |        |
           user-svc cart-svc prod-svc order-svc
           (:8081)  (:8083) (:8082)  (:8084)
              |        |       |         |
           MySQL    Redis  MongoDB    MySQL
          (users)   (cart) (products) (orders)
                                (reviews)
```

### 1.2 技术栈

| 组件 | 技术选型 | 版本 |
|------|----------|------|
| 框架 | Spring Boot | 3.2.2 |
| 语言 | Java | 17 |
| 关系型数据库 | MySQL | 8.0 |
| 文档数据库 | MongoDB | 6.0 |
| 缓存 | Redis | 7 |
| 鉴权 | JWT (jjwt) | 0.12.x |
| 构建工具 | Maven | 3.x |
| 容器化 | Docker | latest |
| 编排部署 | Minikube / K8s | latest |

### 1.3 服务端口分配

| 服务 | 端口 | 数据库 | 说明 |
|------|------|--------|------|
| user-service | 8081 | MySQL `ecommerce.users` | 用户注册/登录/JWT签发 |
| product-service | 8082 | MongoDB `ecommerce` | 商品查询/评价管理 |
| cart-service | 8083 | Redis | 购物车增删查 |
| order-service | 8084 | MySQL `ecommerce.orders` | 订单创建/查询 |
| frontend (Nginx) | 80 | — | 静态资源 + API反向代理 |

### 1.4 核心开发规范

> **禁止硬编码**：代码中禁止出现非默认值外的硬编码。所有可配置项（端口、数据库地址、JWT密钥/过期时间、Redis Key前缀/TTL、跨服务URL、分页默认值等）均须放入 `application.yml`，通过 `@Value` 或 `@ConfigurationProperties` 注入。代码中仅允许保留 fallback 默认值。

---

## 二、公共模块定义

> 以下公共模块需在每个微服务中各自维护一份（各服务独立，不引入公共模块依赖）。

### 2.1 统一响应体 `ApiResponse<T>`

```java
package com.ecommerce.common;

public class ApiResponse<T> {
    private int code;       // 业务状态码
    private String message; // 提示信息
    private T data;         // 业务数据
    private long timestamp; // 响应时间戳（毫秒）

    public ApiResponse() {
        this.timestamp = System.currentTimeMillis();
    }

    public ApiResponse(int code, String message, T data) {
        this.code = code;
        this.message = message;
        this.data = data;
        this.timestamp = System.currentTimeMillis();
    }

    // ===== 静态工厂方法 =====

    public static <T> ApiResponse<T> success(T data) {
        return new ApiResponse<>(200, "success", data);
    }

    public static <T> ApiResponse<T> success(String message, T data) {
        return new ApiResponse<>(200, message, data);
    }

    public static <T> ApiResponse<T> success(String message) {
        return new ApiResponse<>(200, message, null);
    }

    public static <T> ApiResponse<T> error(int code, String message) {
        return new ApiResponse<>(code, message, null);
    }

    // ===== Getter =====
    public int getCode() { return code; }
    public String getMessage() { return message; }
    public T getData() { return data; }
    public long getTimestamp() { return timestamp; }
}
```

### 2.2 分页响应结构 `PageResponse<T>`

```java
package com.ecommerce.common;

import java.util.List;

public class PageResponse<T> {
    private List<T> content;
    private int page;
    private int size;
    private long totalElements;
    private int totalPages;

    public PageResponse(List<T> content, int page, int size, long totalElements) {
        this.content = content;
        this.page = page;
        this.size = size;
        this.totalElements = totalElements;
        this.totalPages = size > 0 ? (int) Math.ceil((double) totalElements / size) : 0;
    }

    // ===== Getter =====
    public List<T> getContent() { return content; }
    public int getPage() { return page; }
    public int getSize() { return size; }
    public long getTotalElements() { return totalElements; }
    public int getTotalPages() { return totalPages; }
}
```

### 2.3 自定义业务异常 `BusinessException`

```java
package com.ecommerce.common;

public class BusinessException extends RuntimeException {
    private final int code;

    public BusinessException(int code, String message) {
        super(message);
        this.code = code;
    }

    public int getCode() { return code; }
}
```

### 2.4 全局异常处理器 `GlobalExceptionHandler`

```java
package com.ecommerce.common;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

@RestControllerAdvice
public class GlobalExceptionHandler {

    private static final Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class);

    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ApiResponse<?>> handleBusiness(BusinessException e) {
        return ResponseEntity.ok(ApiResponse.error(e.getCode(), e.getMessage()));
    }

    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<ApiResponse<?>> handleIllegalArg(IllegalArgumentException e) {
        return ResponseEntity.ok(ApiResponse.error(400, e.getMessage()));
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<ApiResponse<?>> handleException(Exception e) {
        log.error("未捕获异常", e);
        return ResponseEntity.ok(ApiResponse.error(500, "服务器内部错误，请稍后重试"));
    }
}
```

### 2.5 业务状态码规范

| 状态码 | 含义 | 典型场景 |
|--------|------|----------|
| 200 | 成功 | 查询/创建/更新/删除成功 |
| 400 | 参数错误/业务校验失败 | 用户名已存在、密码错误、购物车为空、库存不足 |
| 401 | 未认证 | 未携带Token、Token过期、Token无效 |
| 403 | 无权限 | 用户试图操作他人数据（预留） |
| 404 | 资源不存在 | 商品/订单/用户不存在 |
| 500 | 服务器内部错误 | 未捕获异常、数据库连接失败 |

> **注意**：HTTP 状态码统一返回 200（传输层成功），业务结果通过 `ApiResponse.code` 区分。

---

## 三、配置外部化规范

### 3.1 配置项总览

以下所有可配置项**禁止在代码中硬编码**，必须通过 `application.yml` 管理：

| 配置项 | 所属服务 | application.yml Key | 说明 |
|--------|----------|---------------------|------|
| JWT 密钥 | user-service | `app.jwt.secret` | 签名密钥 |
| JWT 过期时间 | user-service | `app.jwt.expiration` | 单位：毫秒，默认 86400000（24h） |
| Redis Key 前缀 | cart-service | `app.cart.key-prefix` | 默认 `cart:user:` |
| Redis TTL | cart-service | `app.cart.ttl-days` | 默认 7 天 |
| 分页默认大小 | product-service | `app.product.default-size` | 默认 10 |
| 跨服务：cart-service URL | order-service | `app.service.cart-url` | 如 `http://cart-service:8083` |
| 跨服务：product-service URL | order-service | `app.service.product-url` | 如 `http://product-service:8082` |
| 数据库连接 | 各服务 | `spring.datasource.*` / `spring.data.mongodb.*` / `spring.redis.*` | 通过环境变量注入 |

### 3.2 各服务 `application.yml` 完整模板

#### user-service

```yaml
server:
  port: ${SERVER_PORT:8081}

spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/ecommerce}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:rootpassword}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: ${JPA_SHOW_SQL:true}

app:
  jwt:
    secret: ${JWT_SECRET:myDefaultJwtSecretKeyForDevelopmentOnly123456}
    expiration: ${JWT_EXPIRATION:86400000}  # 24小时（毫秒）
```

#### product-service

```yaml
server:
  port: ${SERVER_PORT:8082}

spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/ecommerce}

app:
  product:
    default-size: ${PRODUCT_DEFAULT_SIZE:10}
```

#### cart-service

```yaml
server:
  port: ${SERVER_PORT:8083}

spring:
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}

app:
  cart:
    key-prefix: ${CART_KEY_PREFIX:cart:user:}
    ttl-days: ${CART_TTL_DAYS:7}
```

#### order-service

```yaml
server:
  port: ${SERVER_PORT:8084}

spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/ecommerce}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:rootpassword}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: ${JPA_SHOW_SQL:true}

app:
  service:
    cart-url: ${CART_SERVICE_URL:http://localhost:8083}
    product-url: ${PRODUCT_SERVICE_URL:http://localhost:8082}
  jwt:
    secret: ${JWT_SECRET:myDefaultJwtSecretKeyForDevelopmentOnly123456}
```

### 3.3 `@Value` 注入示例

```java
// JwtUtil.java (user-service)
@Value("${app.jwt.secret}")
private String jwtSecret;

@Value("${app.jwt.expiration:86400000}")
private long jwtExpiration;

// CartServiceImpl.java (cart-service)
@Value("${app.cart.key-prefix:cart:user:}")
private String cartKeyPrefix;

@Value("${app.cart.ttl-days:7}")
private long cartTtlDays;

// ProductServiceImpl.java (product-service)
@Value("${app.product.default-size:10}")
private int defaultPageSize;

// OrderServiceImpl.java (order-service)
@Value("${app.service.cart-url}")
private String cartServiceUrl;

@Value("${app.service.product-url}")
private String productServiceUrl;
```

---

## 四、user-service API 实现规格

### 4.1 服务信息

| 项目 | 值 |
|------|-----|
| 端口 | 8081 |
| 数据库 | MySQL `ecommerce.users` |
| 基础路径 | `/users` |
| 特殊职责 | 用户注册/登录、JWT签发 |

### 4.2 数据模型

**`users` 表：**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(100) | NOT NULL, `@JsonIgnore` | 登录密码（BCrypt加密） |
| pay_password | VARCHAR(100) | `@JsonIgnore` | 支付密码（BCrypt加密） |
| balance | DECIMAL(10,2) | DEFAULT 0.00 | 账户余额 |
| phone | VARCHAR(20) | | 手机号 |
| email | VARCHAR(50) | | 邮箱 |
| avatar | VARCHAR(200) | | 头像URL |
| role | VARCHAR(20) | DEFAULT 'USER' | 角色：USER/MERCHANT/ADMIN |

**`addresses` 表（新增）：**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 地址ID |
| user_id | INT | NOT NULL | 用户ID |
| receiver_name | VARCHAR(50) | NOT NULL | 收件人 |
| phone | VARCHAR(20) | NOT NULL | 手机号 |
| province | VARCHAR(50) | NOT NULL | 省 |
| city | VARCHAR(50) | NOT NULL | 市 |
| district | VARCHAR(50) | NOT NULL | 区 |
| detail_address | VARCHAR(200) | NOT NULL | 详细地址 |
| is_default | BOOLEAN | DEFAULT FALSE | 是否默认地址 |

**实体改造 — `User.java`：**

```java
import com.fasterxml.jackson.annotation.JsonIgnore;

@JsonIgnore  // 添加到 password 和 payPassword 字段上
private String password;

@JsonIgnore
private String payPassword;

// 新增字段
private String phone;
private String email;
private String avatar;
private String role;  // USER, MERCHANT, ADMIN
```

### 4.3 DTO 定义

**`UserRequest.java`（更新）：**

```java
public record UserRequest(
    String username, 
    String password,
    String role,           // 可选：USER/MERCHANT/ADMIN
    String payPassword,    // 可选：支付密码
    String phone,          // 可选
    String email           // 可选
) {}
```

**`LoginResponse.java`（更新）：**

```java
public record LoginResponse(
    String token, 
    Integer userId, 
    String username, 
    BigDecimal balance,
    String role  // 新增：USER/MERCHANT/ADMIN
) {}
```

**新增 DTO：**

```java
// ProfileRequest.java
public record ProfileRequest(String username, String phone, String email) {}

// PasswordRequest.java
public record PasswordRequest(String oldPassword, String newPassword) {}

// PayPasswordRequest.java
public record PayPasswordRequest(String oldPayPassword, String newPayPassword) {}

// ResetPasswordRequest.java
public record ResetPasswordRequest(String type, String newPassword) {} // type: LOGIN/PAY

// AddressRequest.java
public record AddressRequest(
    String receiverName, String phone, 
    String province, String city, String district, 
    String detailAddress, Boolean isDefault
) {}
```

### 4.4 新增组件

#### `JwtUtil.java`

```java
@Component
public class JwtUtil {

    @Value("${app.jwt.secret}")
    private String jwtSecret;

    @Value("${app.jwt.expiration:86400000}")
    private long jwtExpiration;

    public String generateToken(Integer userId, String username) {
        return Jwts.builder()
                .subject(username)
                .claim("userId", userId)
                .issuedAt(new Date())
                .expiration(new Date(System.currentTimeMillis() + jwtExpiration))
                .signWith(getSigningKey())
                .compact();
    }

    public Claims parseToken(String token) {
        return Jwts.parser()
                .setSigningKey(getSigningKey())
                .build()
                .parseClaimsJws(token)
                .getBody();
    }

    public boolean validateToken(String token) {
        try {
            parseToken(token);
            return true;
        } catch (JwtException e) {
            return false;
        }
    }

    private SecretKey getSigningKey() {
        return Keys.hmacShaKeyFor(jwtSecret.getBytes(StandardCharsets.UTF_8));
    }
}
```

#### `UserService` 接口改造

```java
public interface UserService {
    User register(String username, String password);
    LoginResponse login(String username, String password);  // 返回值改为 LoginResponse
}
```

#### `UserServiceImpl` 改造

```java
@Override
public LoginResponse login(String username, String password) {
    User user = userRepository.findByUsername(username)
            .orElseThrow(() -> new BusinessException(400, "用户不存在"));
    if (!user.getPassword().equals(password)) {
        throw new BusinessException(400, "密码错误");
    }
    String token = jwtUtil.generateToken(user.getId(), user.getUsername());
    return new LoginResponse(token, user.getId(), user.getUsername(), user.getBalance());
}

@Override
public User register(String username, String password) {
    if (userRepository.findByUsername(username).isPresent()) {
        throw new BusinessException(400, "用户名已存在");
    }
    User user = new User();
    user.setUsername(username);
    user.setPassword(password);
    user.setBalance(new BigDecimal("1000.00"));
    return userRepository.save(user);
}
```

### 4.5 API 接口定义

#### `POST /users/register` — 用户注册

**请求：**

```
POST /users/register
Content-Type: application/json

{
  "username": "张三",
  "password": "123456"
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "张三",
    "balance": 1000.00
  },
  "timestamp": 1752105600000
}
```

> `data` 中不含 `password`（`@JsonIgnore` 生效）。

**错误响应（400）：**

```json
{
  "code": 400,
  "message": "用户名已存在",
  "data": null,
  "timestamp": 1752105600000
}
```

**Controller 实现：**

```java
@PostMapping("/register")
public ResponseEntity<ApiResponse<User>> register(@RequestBody UserRequest request) {
    User user = userService.register(request.username(), request.password());
    return ResponseEntity.ok(ApiResponse.success(user));
}
```

---

#### `POST /users/login` — 用户登录

**请求：**

```
POST /users/login
Content-Type: application/json

{
  "username": "张三",
  "password": "123456"
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1,
    "username": "张三",
    "balance": 1000.00
  },
  "timestamp": 1752105600000
}
```

**错误响应（400）：**

```json
{ "code": 400, "message": "密码错误", "data": null, "timestamp": 1752105600000 }
```

```json
{ "code": 400, "message": "用户不存在", "data": null, "timestamp": 1752105600000 }
```

**Controller 实现：**

```java
@PostMapping("/login")
public ResponseEntity<ApiResponse<LoginResponse>> login(@RequestBody UserRequest request) {
    LoginResponse response = userService.login(request.username(), request.password());
    return ResponseEntity.ok(ApiResponse.success(response));
}
```

---

#### `GET /users/{id}` — 查询用户信息

**请求：**

```
GET /users/2
Authorization: Bearer <token>
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 2,
    "username": "testuser",
    "balance": 1000.00,
    "phone": "13800138000",
    "email": "test@example.com",
    "avatar": null,
    "role": "USER"
  },
  "timestamp": 1783751647891
}
```

---

#### `PUT /users/{id}/profile` — 修改用户资料

**请求：**

```
PUT /users/2/profile
Authorization: Bearer <token>
Content-Type: application/json

{
  "username": "newname",
  "phone": "13800138000",
  "email": "test@example.com"
}
```

---

#### `PUT /users/{id}/password` — 修改登录密码

**请求：**

```
PUT /users/2/password
Authorization: Bearer <token>
Content-Type: application/json

{
  "oldPassword": "123456",
  "newPassword": "654321"
}
```

---

#### `PUT /users/{id}/pay-password` — 修改支付密码

**请求：**

```
PUT /users/2/pay-password
Authorization: Bearer <token>
Content-Type: application/json

{
  "oldPayPassword": "123456",
  "newPayPassword": "654321"
}
```

---

#### `POST /users/{id}/reset-password` — 重置密码

**请求：**

```
POST /users/2/reset-password
Authorization: Bearer <token>
Content-Type: application/json

{
  "type": "LOGIN",
  "newPassword": "123456"
}
```

---

#### `POST /users/{id}/avatar` — 上传头像

**请求：**

```
POST /users/2/avatar
Authorization: Bearer <token>
Content-Type: multipart/form-data

file: [图片文件]
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": "http://localhost:8081/uploads/avatars/2_1783751647891.jpg",
  "timestamp": 1783751647891
}
```

---

#### `POST /users/{id}/addresses` — 新增地址

**请求：**

```
POST /users/2/addresses
Authorization: Bearer <token>
Content-Type: application/json

{
  "receiverName": "Test",
  "phone": "13800138000",
  "province": "Beijing",
  "city": "Beijing",
  "district": "Chaoyang",
  "detailAddress": "Street 1",
  "isDefault": true
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 2,
    "receiverName": "Test",
    "phone": "13800138000",
    "province": "Beijing",
    "city": "Beijing",
    "district": "Chaoyang",
    "detailAddress": "Street 1",
    "isDefault": true
  },
  "timestamp": 1783751665224
}
```

---

#### `GET /users/{id}/addresses` — 获取地址列表

**请求：**

```
GET /users/2/addresses
Authorization: Bearer <token>
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 2,
      "receiverName": "Test",
      "phone": "13800138000",
      "province": "Beijing",
      "city": "Beijing",
      "district": "Chaoyang",
      "detailAddress": "Street 1",
      "isDefault": true
    }
  ],
  "timestamp": 1783751836950
}
```

---

#### `PUT /users/{id}/addresses/{addressId}` — 修改地址

**请求：**

```
PUT /users/2/addresses/1
Authorization: Bearer <token>
Content-Type: application/json

{
  "receiverName": "New Name",
  "phone": "13900139000",
  "province": "Shanghai",
  "city": "Shanghai",
  "district": "Pudong",
  "detailAddress": "New Street",
  "isDefault": false
}
```

---

#### `DELETE /users/{id}/addresses/{addressId}` — 删除地址

**请求：**

```
DELETE /users/2/addresses/1
Authorization: Bearer <token>
```

---

#### `PUT /users/{id}/addresses/{addressId}/default` — 设置默认地址

**请求：**

```
PUT /users/2/addresses/1/default
Authorization: Bearer <token>
```

---

#### `DELETE /users/{id}/self` — 用户注销

**请求：**

```
DELETE /users/2/self
Authorization: Bearer <token>
```

---

#### `PUT /users/{id}` — 管理员修改用户

**请求：**

```
PUT /users/2
Authorization: Bearer <token> (ADMIN)
Content-Type: application/json

{
  "username": "updated",
  "balance": 2000.00,
  "role": "MERCHANT"
}
```

---

#### `PUT /users/{id}/role` — 管理员修改角色

**请求：**

```
PUT /users/2/role
Authorization: Bearer <token> (ADMIN)
Content-Type: application/json

{
  "role": "MERCHANT"
}
```

---

#### `DELETE /users/{id}` — 管理员删除用户

**请求：**

```
DELETE /users/2
Authorization: Bearer <token> (ADMIN)
```

### 4.6 pom.xml 新增依赖

```xml
<!-- JWT -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.6</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
```

---

## 五、product-service API 实现规格

### 5.1 服务信息

| 项目 | 值 |
|------|-----|
| 端口 | 8082 |
| 数据库 | MongoDB `ecommerce`（集合：`products`、`reviews`） |
| 基础路径 | `/products`、`/reviews` |
| 特殊职责 | 商品查询（分页）、评价管理 |

### 5.2 数据模型

**`products` 集合：**

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | Integer | 商品ID（手动指定，非 ObjectId） |
| name | String | 商品名称 |
| price | BigDecimal | 价格 |
| stock | Integer | 库存 |
| category | String | 分类 |
| merchantId | Integer | 商家ID（新增） |
| description | String | 商品描述（新增） |
| imageUrl | String | 商品图片URL（新增） |
| attributes | Map<String, Object> | 动态属性（MongoDB 核心优势） |

**`reviews` 集合：**

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | String | MongoDB 自动生成 ObjectId |
| productId | Integer | 关联商品ID |
| username | String | 评价用户名 |
| content | String | 评价内容 |
| rating | Integer | 评分（1-5） |

**索引：** `db.reviews.createIndex({productId: 1})`

### 5.3 改造点

- `ProductServiceImpl` 新增 `getAllProducts(int page, int size)` 方法（分页查询）
- `ProductController` 新增 `GET /products` 接口
- 所有 Controller 返回类型改为 `ResponseEntity<ApiResponse<T>>`
- 分页默认大小通过 `@Value` 注入，禁止硬编码

### 5.4 Service 层新增方法

```java
// ProductServiceImpl.java
@Value("${app.product.default-size:10}")
private int defaultPageSize;

public PageResponse<Product> getAllProducts(int page, int size) {
    if (size <= 0) size = defaultPageSize;
    List<Product> all = productRepository.findAll();
    int total = all.size();
    int from = page * size;
    int to = Math.min(from + size, total);
    List<Product> content = (from < total) ? all.subList(from, to) : List.of();
    return new PageResponse<>(content, page, size, total);
}
```

### 5.5 API 接口定义

#### `GET /products` — 商品列表（分页）

**请求：**

```
GET /products?page=0&size=10
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | int | 否 | 0 | 页码（从 0 开始） |
| size | int | 否 | 10 | 每页条数 |

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "content": [
      {
        "id": 1,
        "name": "华为 Mate 60",
        "price": 6999.00,
        "stock": 50,
        "category": "Electronics",
        "attributes": { "color": "雅川青", "storage": "512GB", "network": "5G" }
      }
    ],
    "page": 0,
    "size": 10,
    "totalElements": 3,
    "totalPages": 1
  },
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@GetMapping("/products")
public ResponseEntity<ApiResponse<PageResponse<Product>>> getProducts(
        @RequestParam(defaultValue = "0") int page,
        @RequestParam(defaultValue = "${app.product.default-size:10}") int size) {
    PageResponse<Product> result = productService.getAllProducts(page, size);
    return ResponseEntity.ok(ApiResponse.success(result));
}
```

---

#### `GET /products/{id}` — 商品详情

**请求：**

```
GET /products/1
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "华为 Mate 60",
    "price": 6999.00,
    "stock": 50,
    "category": "Electronics",
    "attributes": { "color": "雅川青", "storage": "512GB" }
  },
  "timestamp": 1752105600000
}
```

**错误响应（404）：**

```json
{ "code": 404, "message": "商品不存在", "data": null, "timestamp": 1752105600000 }
```

**Controller：**

```java
@GetMapping("/products/{id}")
public ResponseEntity<ApiResponse<Product>> getProduct(@PathVariable Integer id) {
    Product product = productService.getProduct(id);
    return ResponseEntity.ok(ApiResponse.success(product));
}
```

> Service 层抛出 `BusinessException(404, "商品不存在")` 时由 `GlobalExceptionHandler` 统一处理。

---

#### `POST /reviews` — 发表评价

**请求：**

```
POST /reviews
Content-Type: application/json

{
  "productId": 1,
  "username": "张三",
  "content": "非常好用，推荐购买！",
  "rating": 5
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "66a1b2c3d4e5f6a7b8c9d0e1",
    "productId": 1,
    "username": "张三",
    "content": "非常好用，推荐购买！",
    "rating": 5
  },
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@PostMapping("/reviews")
public ResponseEntity<ApiResponse<Review>> addReview(@RequestBody Review review) {
    Review saved = productService.addReview(review);
    return ResponseEntity.ok(ApiResponse.success(saved));
}
```

---

#### `GET /reviews/{productId}` — 获取商品评价列表

**请求：**

```
GET /reviews/1
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "66a1b2c3d4e5f6a7b8c9d0e1",
      "productId": 1,
      "username": "张三",
      "content": "非常好用，推荐购买！",
      "rating": 5
    }
  ],
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@GetMapping("/reviews/{productId}")
public ResponseEntity<ApiResponse<List<Review>>> getReviews(@PathVariable Integer productId) {
    List<Review> reviews = productService.getReviews(productId);
    return ResponseEntity.ok(ApiResponse.success(reviews));
}
```

---

#### `POST /products/upload` — 上传商品图片（新增）

**请求：**

```
POST /products/upload
Authorization: Bearer <token>
Content-Type: multipart/form-data

file: [图片文件]
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": "http://localhost:8082/uploads/products/1_1783751647891.jpg",
  "timestamp": 1783751647891
}
```

---

#### `POST /products` — 商家发布商品（新增）

**请求：**

```
POST /products
Authorization: Bearer <token> (MERCHANT/ADMIN)
Content-Type: application/json

{
  "name": "新商品",
  "price": 999.00,
  "stock": 100,
  "category": "Electronics",
  "merchantId": 3,
  "description": "商品描述",
  "imageUrl": "http://localhost:8082/uploads/products/1.jpg",
  "attributes": {"color": "黑色"}
}
```

---

#### `PUT /products/{id}` — 商家修改商品（新增）

**请求：**

```
PUT /products/1
Authorization: Bearer <token> (MERCHANT/ADMIN)
Content-Type: application/json

{
  "price": 899.00,
  "stock": 80,
  "description": "更新后的描述"
}
```

---

#### `DELETE /products/{id}` — 商家删除商品（新增）

**请求：**

```
DELETE /products/1
Authorization: Bearer <token> (MERCHANT/ADMIN)
```

---

#### `GET /products/merchant/{merchantId}` — 商家商品列表（新增）

**请求：**

```
GET /products/merchant/3
Authorization: Bearer <token>
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "华为 Mate 60",
      "price": 6999.00,
      "stock": 50,
      "category": "Electronics",
      "merchantId": 3,
      "description": "商品描述",
      "imageUrl": "http://localhost:8082/uploads/products/1.jpg"
    }
  ],
  "timestamp": 1783751647891
}
```

---

## 六、cart-service API 实现规格

### 6.1 服务信息

| 项目 | 值 |
|------|-----|
| 端口 | 8083 |
| 数据库 | Redis（Hash 结构） |
| 基础路径 | `/cart` |
| 特殊职责 | 购物车增删查清 |

### 6.2 Redis 存储设计

| 项目 | 说明 |
|------|------|
| Key 格式 | `{cartKeyPrefix}{userId}` — 前缀从配置读取，默认 `cart:user:` |
| Hash Field | `{productId}` |
| Hash Value | `{quantity}` |
| TTL | 从配置读取，默认 7 天 |

### 6.3 改造点

- `CartServiceImpl` 中 `CART_PREFIX` 常量改为 `@Value` 注入
- 新增 TTL 设置（`redisTemplate.expire(key, ttlDays, TimeUnit.DAYS)`）
- `CartController` 返回类型改为 `ResponseEntity<ApiResponse<T>>`
- 新增 `DELETE /cart/{userId}` 清空购物车接口
- `GET /cart/{userId}` 响应结构化（当前返回原始 Map，改为结构化 DTO）

### 6.4 DTO 定义

**`CartResponse.java`（新增）：**

```java
public class CartResponse {
    private Long userId;
    private List<CartItem> items;

    public CartResponse(Long userId, List<CartItem> items) {
        this.userId = userId;
        this.items = items;
    }

    public Long getUserId() { return userId; }
    public List<CartItem> getItems() { return items; }
}
```

**`CartItem.java`（新增）：**

```java
public class CartItem {
    private Long productId;
    private Integer quantity;

    public CartItem(Long productId, Integer quantity) {
        this.productId = productId;
        this.quantity = quantity;
    }

    public Long getProductId() { return productId; }
    public Integer getQuantity() { return quantity; }
}
```

### 6.5 Service 层改造

```java
@Service
public class CartServiceImpl implements CartService {

    @Value("${app.cart.key-prefix:cart:user:}")
    private String cartKeyPrefix;

    @Value("${app.cart.ttl-days:7}")
    private long cartTtlDays;

    @Autowired
    private RedisTemplate<String, Object> redisTemplate;

    private String buildKey(Long userId) {
        return cartKeyPrefix + userId;
    }

    @Override
    public void addToCart(Long userId, Long productId, Integer quantity) {
        String key = buildKey(userId);
        redisTemplate.opsForHash().put(key, productId.toString(), quantity);
        redisTemplate.expire(key, cartTtlDays, TimeUnit.DAYS);
    }

    @Override
    public CartResponse getCart(Long userId) {
        String key = buildKey(userId);
        Map<Object, Object> entries = redisTemplate.opsForHash().entries(key);
        List<CartItem> items = entries.entrySet().stream()
                .map(e -> new CartItem(Long.parseLong(e.getKey().toString()),
                                       Integer.parseInt(e.getValue().toString())))
                .toList();
        return new CartResponse(userId, items);
    }

    @Override
    public void removeFromCart(Long userId, Long productId) {
        String key = buildKey(userId);
        redisTemplate.opsForHash().delete(key, productId.toString());
    }

    @Override
    public void clearCart(Long userId) {
        String key = buildKey(userId);
        redisTemplate.delete(key);
    }
}
```

### 6.6 API 接口定义

#### `POST /cart/add` — 添加商品到购物车

**请求：**

```
POST /cart/add
Content-Type: application/json

{
  "userId": 1,
  "productId": 2,
  "quantity": 3
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "添加成功",
  "data": null,
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@PostMapping("/add")
public ResponseEntity<ApiResponse<Void>> add(@RequestBody CartItemRequest request) {
    cartService.addToCart(request.getUserId(), request.getProductId(), request.getQuantity());
    return ResponseEntity.ok(ApiResponse.success("添加成功"));
}
```

---

#### `GET /cart/{userId}` — 获取用户购物车

**请求：**

```
GET /cart/1
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 1,
    "items": [
      { "productId": 1, "quantity": 2 },
      { "productId": 3, "quantity": 1 }
    ]
  },
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@GetMapping("/{userId}")
public ResponseEntity<ApiResponse<CartResponse>> get(@PathVariable Long userId) {
    CartResponse cart = cartService.getCart(userId);
    return ResponseEntity.ok(ApiResponse.success(cart));
}
```

---

#### `DELETE /cart/{userId}/{productId}` — 移除购物车中单个商品

**请求：**

```
DELETE /cart/1/2
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "移除成功",
  "data": null,
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@DeleteMapping("/{userId}/{productId}")
public ResponseEntity<ApiResponse<Void>> delete(
        @PathVariable Long userId, @PathVariable Long productId) {
    cartService.removeFromCart(userId, productId);
    return ResponseEntity.ok(ApiResponse.success("移除成功"));
}
```

---

#### `DELETE /cart/{userId}` — 清空购物车（新增）

**请求：**

```
DELETE /cart/1
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "清空成功",
  "data": null,
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@DeleteMapping("/{userId}")
public ResponseEntity<ApiResponse<Void>> clear(@PathVariable Long userId) {
    cartService.clearCart(userId);
    return ResponseEntity.ok(ApiResponse.success("清空成功"));
}
```

---

#### `PUT /cart/{userId}/{productId}` — 更新购物车商品数量（新增）

**请求：**

```
PUT /cart/2/1
Authorization: Bearer <token>
Content-Type: application/json

{
  "quantity": 5
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "数量更新成功",
  "data": null,
  "timestamp": 1783751647891
}
```

---

#### `POST /cart/{userId}/remove-selected` — 移除勾选商品（新增）

**请求：**

```
POST /cart/2/remove-selected
Authorization: Bearer <token>
Content-Type: application/json

{
  "productIds": [1, 2, 3]
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "勾选商品移除成功",
  "data": null,
  "timestamp": 1783751647891
}
```

---

## 七、order-service API 实现规格（新增服务）

### 7.1 服务信息

| 项目 | 值 |
|------|-----|
| 端口 | 8084 |
| 数据库 | MySQL `ecommerce`（表：`orders`、`order_items`） |
| 基础路径 | `/orders` |
| 特殊职责 | 订单创建（跨服务调用）、订单查询 |
| 跨服务依赖 | cart-service（获取/清空购物车）、product-service（查商品价格/扣库存） |

### 7.2 项目结构

```
ecommerce-project/order-service/
├── src/main/java/com/ecommerce/order/
│   ├── OrderApplication.java
│   ├── controller/
│   │   └── OrderController.java
│   ├── entity/
│   │   ├── Order.java
│   │   └── OrderItem.java
│   ├── repository/
│   │   ├── OrderRepository.java
│   │   └── OrderItemRepository.java
│   ├── service/
│   │   ├── OrderService.java
│   │   └── impl/OrderServiceImpl.java
│   └── dto/
│       ├── CreateOrderRequest.java
│       └── OrderResponse.java
├── src/main/resources/
│   └── application.yml
├── Dockerfile
└── pom.xml
```

### 7.3 数据模型

**`orders` 表：**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 订单ID |
| user_id | INT | NOT NULL | 用户ID |
| merchant_id | INT | | 商家ID（新增） |
| address_id | INT | | 收货地址ID（新增） |
| total_price | DECIMAL(10,2) | NOT NULL | 订单总价 |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'UNPAID' | 状态：UNPAID/PAID/SHIPPED/IN_TRANSIT/DELIVERED/COMPLETED/CANCELLED |
| cart_snapshot | VARCHAR(100) | | 购物车快照（防重复提交） |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 更新时间 |

**`order_items` 表：**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 订单项ID |
| order_id | BIGINT | NOT NULL, FK→orders.id | 所属订单 |
| product_id | INT | NOT NULL | 商品ID |
| product_name | VARCHAR(100) | NOT NULL | 商品名称（冗余快照） |
| price | DECIMAL(10,2) | NOT NULL | 下单时单价（冗余快照） |
| quantity | INT | NOT NULL | 购买数量 |

### 7.4 实体定义

**`Order.java`：**

```java
@Entity
@Table(name = "orders")
public class Order {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "user_id", nullable = false)
    private Integer userId;

    @Column(name = "total_price", nullable = false, precision = 10, scale = 2)
    private BigDecimal totalPrice;

    @Column(nullable = false, length = 20)
    private String status = "CREATED";

    @Column(name = "created_at", nullable = false)
    private LocalDateTime createdAt;

    @Column(name = "updated_at", nullable = false)
    private LocalDateTime updatedAt;

    @OneToMany(mappedBy = "order", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
    private List<OrderItem> items = new ArrayList<>();

    @PrePersist
    protected void onCreate() {
        createdAt = LocalDateTime.now();
        updatedAt = LocalDateTime.now();
    }

    @PreUpdate
    protected void onUpdate() {
        updatedAt = LocalDateTime.now();
    }

    // ===== Getter & Setter =====
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public Integer getUserId() { return userId; }
    public void setUserId(Integer userId) { this.userId = userId; }
    public BigDecimal getTotalPrice() { return totalPrice; }
    public void setTotalPrice(BigDecimal totalPrice) { this.totalPrice = totalPrice; }
    public String getStatus() { return status; }
    public void setStatus(String status) { this.status = status; }
    public LocalDateTime getCreatedAt() { return createdAt; }
    public LocalDateTime getUpdatedAt() { return updatedAt; }
    public List<OrderItem> getItems() { return items; }
    public void setItems(List<OrderItem> items) { this.items = items; }
}
```

**`OrderItem.java`：**

```java
@Entity
@Table(name = "order_items")
public class OrderItem {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "order_id", nullable = false)
    @JsonIgnore
    private Order order;

    @Column(name = "product_id", nullable = false)
    private Integer productId;

    @Column(name = "product_name", nullable = false, length = 100)
    private String productName;

    @Column(nullable = false, precision = 10, scale = 2)
    private BigDecimal price;

    @Column(nullable = false)
    private Integer quantity;

    // ===== Getter & Setter =====
    public Long getId() { return id; }
    public Order getOrder() { return order; }
    public void setOrder(Order order) { this.order = order; }
    public Integer getProductId() { return productId; }
    public void setProductId(Integer productId) { this.productId = productId; }
    public String getProductName() { return productName; }
    public void setProductName(String productName) { this.productName = productName; }
    public BigDecimal getPrice() { return price; }
    public void setPrice(BigDecimal price) { this.price = price; }
    public Integer getQuantity() { return quantity; }
    public void setQuantity(Integer quantity) { this.quantity = quantity; }
}
```

### 7.5 DTO 定义

**`CreateOrderRequest.java`：**

```java
public record CreateOrderRequest(Integer userId) {}
```

**`OrderResponse.java`（更新）：**

```java
public class OrderResponse {
    private Long id;
    private Integer userId;
    private Integer merchantId;  // 新增
    private Integer addressId;   // 新增
    private BigDecimal totalPrice;
    private String status;
    private List<OrderItemResponse> items;
    private LocalDateTime createdAt;

    public static class OrderItemResponse {
        private Integer productId;
        private String productName;
        private BigDecimal price;
        private Integer quantity;

        // Getter & Setter
        public Integer getProductId() { return productId; }
        public void setProductId(Integer productId) { this.productId = productId; }
        public String getProductName() { return productName; }
        public void setProductName(String productName) { this.productName = productName; }
        public BigDecimal getPrice() { return price; }
        public void setPrice(BigDecimal price) { this.price = price; }
        public Integer getQuantity() { return quantity; }
        public void setQuantity(Integer quantity) { this.quantity = quantity; }
    }

    // Getter & Setter
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public Integer getUserId() { return userId; }
    public void setUserId(Integer userId) { this.userId = userId; }
    public Integer getMerchantId() { return merchantId; }
    public void setMerchantId(Integer merchantId) { this.merchantId = merchantId; }
    public Integer getAddressId() { return addressId; }
    public void setAddressId(Integer addressId) { this.addressId = addressId; }
    public BigDecimal getTotalPrice() { return totalPrice; }
    public void setTotalPrice(BigDecimal totalPrice) { this.totalPrice = totalPrice; }
    public String getStatus() { return status; }
    public void setStatus(String status) { this.status = status; }
    public List<OrderItemResponse> getItems() { return items; }
    public void setItems(List<OrderItemResponse> items) { this.items = items; }
    public LocalDateTime getCreatedAt() { return createdAt; }
    public void setCreatedAt(LocalDateTime createdAt) { this.createdAt = createdAt; }
}
```

**新增 DTO：**

```java
// SelectedOrderRequest.java
public record SelectedOrderRequest(Integer userId, List<Integer> productIds) {}

// UpdateStatusRequest.java
public record UpdateStatusRequest(String status) {}

// PayRequest.java
public record PayRequest(String payPassword, Integer addressId) {}
```

### 7.6 Repository 定义

```java
public interface OrderRepository extends JpaRepository<Order, Long> {
    List<Order> findByUserIdOrderByCreatedAtDesc(Integer userId);
}

public interface OrderItemRepository extends JpaRepository<OrderItem, Long> {
}
```

### 7.7 创建订单流程

```
1. 调用 cart-service GET /cart/{userId} 获取购物车
2. 校验购物车是否为空（空则抛出 BusinessException(400, "购物车为空")）
3. 调用 product-service GET /products/{productId} 获取每个商品价格
4. 校验库存是否充足（不足则抛出 BusinessException(400, "商品[xxx]库存不足")）
5. 计算总价
6. MySQL 事务内：
   a. 创建 Order 记录
   b. 创建 OrderItem 记录（冗余商品名称和下单时价格）
   c. 扣减商品库存（通过 product-service 接口）
7. 调用 cart-service DELETE /cart/{userId} 清空购物车
8. 返回 OrderResponse
```

### 7.8 Service 层实现

```java
@Service
public class OrderServiceImpl implements OrderService {

    @Value("${app.service.cart-url}")
    private String cartServiceUrl;

    @Value("${app.service.product-url}")
    private String productServiceUrl;

    @Autowired
    private OrderRepository orderRepository;

    @Autowired
    private OrderItemRepository orderItemRepository;

    @Autowired
    private RestTemplate restTemplate;

    @Override
    @Transactional
    public OrderResponse createOrder(Integer userId) {
        // 1. 获取购物车
        String cartUrl = cartServiceUrl + "/cart/" + userId;
        ApiResponse<?> cartResp = restTemplate.getForObject(cartUrl, ApiResponse.class);
        // 解析购物车 items...

        // 2. 校验购物车是否为空
        // if (items.isEmpty()) throw new BusinessException(400, "购物车为空");

        // 3. 逐个查询商品价格并校验库存
        // 4. 计算总价
        // 5. 创建订单 + 订单项
        // 6. 扣减库存（调用 product-service）
        // 7. 清空购物车
        // 8. 返回 OrderResponse

        // ... 具体实现代码 ...
    }

    @Override
    public List<OrderResponse> getOrdersByUserId(Integer userId) {
        List<Order> orders = orderRepository.findByUserIdOrderByCreatedAtDesc(userId);
        return orders.stream().map(this::toResponse).toList();
    }

    @Override
    public OrderResponse getOrderById(Long id) {
        Order order = orderRepository.findById(id)
                .orElseThrow(() -> new BusinessException(404, "订单不存在"));
        return toResponse(order);
    }

    private OrderResponse toResponse(Order order) {
        // Entity -> DTO 转换
    }
}
```

**`RestTemplate` Bean 配置：**

```java
@Configuration
public class AppConfig {
    @Bean
    public RestTemplate restTemplate() {
        return new RestTemplate();
    }
}
```

### 7.9 API 接口定义

#### `POST /orders` — 创建订单

**请求：**

```
POST /orders
Content-Type: application/json

{
  "userId": 1
}
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "totalPrice": 1499.00,
    "status": "CREATED",
    "items": [
      {
        "productId": 2,
        "productName": "机械键盘",
        "price": 499.00,
        "quantity": 3
      }
    ],
    "createdAt": "2026-07-10T10:00:00"
  },
  "timestamp": 1752105600000
}
```

**错误响应：**

```json
{ "code": 400, "message": "购物车为空", "data": null, "timestamp": 1752105600000 }
```

```json
{ "code": 400, "message": "商品[机械键盘]库存不足", "data": null, "timestamp": 1752105600000 }
```

**Controller：**

```java
@PostMapping
public ResponseEntity<ApiResponse<OrderResponse>> createOrder(@RequestBody CreateOrderRequest request) {
    OrderResponse order = orderService.createOrder(request.userId());
    return ResponseEntity.ok(ApiResponse.success(order));
}
```

---

#### `GET /orders/user/{userId}` — 获取用户所有订单

**请求：**

```
GET /orders/user/1
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "totalPrice": 1499.00,
      "status": "CREATED",
      "createdAt": "2026-07-10T10:00:00",
      "itemCount": 1
    }
  ],
  "timestamp": 1752105600000
}
```

**Controller：**

```java
@GetMapping("/user/{userId}")
public ResponseEntity<ApiResponse<List<OrderResponse>>> getOrdersByUser(@PathVariable Integer userId) {
    List<OrderResponse> orders = orderService.getOrdersByUserId(userId);
    return ResponseEntity.ok(ApiResponse.success(orders));
}
```

---

#### `GET /orders/{id}` — 获取订单详情

**请求：**

```
GET /orders/1
```

**成功响应（200）：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "totalPrice": 1499.00,
    "status": "CREATED",
    "items": [
      {
        "productId": 2,
        "productName": "机械键盘",
        "price": 499.00,
        "quantity": 3
      }
    ],
    "createdAt": "2026-07-10T10:00:00"
  },
  "timestamp": 1752105600000
}
```

**错误响应（404）：**

```json
{ "code": 404, "message": "订单不存在", "data": null, "timestamp": 1752105600000 }
```

**Controller：**

```java
@GetMapping("/{id}")
public ResponseEntity<ApiResponse<OrderResponse>> getOrder(@PathVariable Long id) {
    OrderResponse order = orderService.getOrderById(id);
    return ResponseEntity.ok(ApiResponse.success(order));
}
```

### 7.10 pom.xml 依赖

```xml
<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>
</dependencies>
```

---

## 八、JWT 鉴权模块

### 8.1 鉴权流程

```
用户浏览器                 user-service              其他微服务
    |                           |                           |
    |--- POST /users/login ----->|                           |
    |<-- { token, userId, ... }--|                           |
    |                           |                           |
    |--- GET /products (Bearer token) ------------------>|
    |                           |       JwtAuthFilter 验证  |
    |<------------------------ { data } ------------------|
```

1. 用户登录成功后，user-service 签发 JWT（含 userId、username）
2. 后续请求在 Header 携带 `Authorization: Bearer <JWT>`
3. cart/product/order-service 的 `JwtAuthFilter` 拦截验证

### 8.2 JwtUtil 工具类（user-service）

已在第四章 4.4 节定义。关键点：
- 密钥从 `app.jwt.secret` 配置读取（禁止硬编码）
- 过期时间从 `app.jwt.expiration` 配置读取，默认 24h
- Token Payload 包含：`sub`(username)、`userId`(claim)、`iat`、`exp`

### 8.3 JwtAuthFilter 拦截器（cart/product/order-service）

```java
@Component
public class JwtAuthFilter extends OncePerRequestHandlerInterceptor {

    @Value("${app.jwt.secret}")
    private String jwtSecret;

    @Override
    protected boolean preHandle(HttpServletRequest request,
                                HttpServletResponse response,
                                Object handler) throws Exception {
        // OPTIONS 请求直接放行
        if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
            return true;
        }

        String authHeader = request.getHeader("Authorization");
        if (authHeader == null || !authHeader.startsWith("Bearer ")) {
            response.setStatus(HttpServletResponse.SC_OK);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                "{\"code\":401,\"message\":\"请先登录\",\"data\":null,\"timestamp\":"
                + System.currentTimeMillis() + "}");
            return false;
        }

        String token = authHeader.substring(7);
        try {
            Claims claims = Jwts.parser()
                    .setSigningKey(Keys.hmacShaKeyFor(jwtSecret.getBytes(StandardCharsets.UTF_8)))
                    .build()
                    .parseClaimsJws(token)
                    .getBody();
            // 可将 userId 存入 request attribute 供后续使用
            request.setAttribute("userId", claims.get("userId", Integer.class));
            request.setAttribute("username", claims.getSubject());
            return true;
        } catch (JwtException e) {
            response.setStatus(HttpServletResponse.SC_OK);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                "{\"code\":401,\"message\":\"Token无效或已过期\",\"data\":null,\"timestamp\":"
                + System.currentTimeMillis() + "}");
            return false;
        }
    }
}
```

### 8.4 拦截器注册配置

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Autowired
    private JwtAuthFilter jwtAuthFilter;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(jwtAuthFilter)
                .addPathPatterns("/**")          // 拦截所有路径
                .excludePathPatterns(            // 白名单
                    "/users/login",
                    "/users/register"
                );
    }
}
```

### 8.5 各服务 JWT 配置

| 服务 | 配置项 | 说明 |
|------|--------|------|
| user-service | `app.jwt.secret` / `app.jwt.expiration` | 签发 + 验证 |
| cart-service | `app.jwt.secret` | 仅验证 |
| product-service | `app.jwt.secret` | 仅验证 |
| order-service | `app.jwt.secret` | 仅验证 |

> 所有服务的 `app.jwt.secret` 必须相同，否则 Token 无法跨服务验证。

### 8.6 前端 Token 管理约定

- 登录成功后将 token 存入 `localStorage`
- Axios 请求拦截器自动附加 `Authorization: Bearer <token>`
- 收到 `code: 401` 响应时自动清除 token 并跳转登录页

```javascript
// Axios 请求拦截器
axios.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// Axios 响应拦截器
axios.interceptors.response.use(
  response => {
    const { code, message, data } = response.data;
    if (code === 200) return data;
    if (code === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
      return Promise.reject(new Error(message));
    }
    return Promise.reject(new Error(message));
  },
  error => Promise.reject(new Error('网络异常，请检查连接'))
);
```

---

## 九、前端对接契约

### 9.1 页面路由与后端 API 映射

| 前端路由 | 页面 | 对接 API |
|----------|------|----------|
| `/login` | 登录/注册 | `POST /users/register`、`POST /users/login` |
| `/` | 商品列表 | `GET /products?page=0&size=10` |
| `/products/:id` | 商品详情+评价 | `GET /products/{id}`、`GET /reviews/{productId}`、`POST /reviews` |
| `/cart` | 购物车 | `GET /cart/{userId}`、`POST /cart/add`、`DELETE /cart/{userId}/{productId}`、`DELETE /cart/{userId}` |
| `/orders` | 我的订单 | `POST /orders`、`GET /orders/user/{userId}`、`GET /orders/{id}` |

### 9.2 Nginx 反向代理规则

```
location /users    { proxy_pass http://user-service:8081; }
location /products { proxy_pass http://product-service:8082; }
location /reviews  { proxy_pass http://product-service:8082; }
location /cart     { proxy_pass http://cart-service:8083; }
location /orders   { proxy_pass http://order-service:8084; }
location /         { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
```

> `try_files $uri $uri/ /index.html` 解决 React Router 刷新 404 问题。

### 9.3 错误处理规范

- 所有接口统一返回 `ApiResponse` 结构，前端通过 `code` 判断
- `code === 200`：成功，直接使用 `data`
- `code === 400`：业务错误，展示 `message` 给用户
- `code === 401`：未认证，清除 token 并跳转登录页
- `code === 404`：资源不存在，展示提示信息
- `code === 500`：服务器错误，展示通用错误提示

---

## 十、配置与部署参考

### 10.1 环境变量清单

| 变量名 | 所属服务 | 默认值 | 说明 |
|--------|----------|--------|------|
| `MYSQL_URL` | user/order | `jdbc:mysql://localhost:3306/ecommerce` | MySQL 连接地址 |
| `MYSQL_USER` | user/order | `root` | MySQL 用户名 |
| `MYSQL_PASSWORD` | user/order | `rootpassword` | MySQL 密码 |
| `MONGO_URI` | product | `mongodb://localhost:27017/ecommerce` | MongoDB 连接地址 |
| `REDIS_HOST` | cart | `localhost` | Redis 主机 |
| `REDIS_PORT` | cart | `6379` | Redis 端口 |
| `JWT_SECRET` | user/cart/product/order | `myDefaultJwtSecretKeyForDevelopmentOnly123456` | JWT 签名密钥 |
| `JWT_EXPIRATION` | user | `86400000` | JWT 过期时间（毫秒） |
| `CART_SERVICE_URL` | order | `http://localhost:8083` | 购物车服务地址 |
| `PRODUCT_SERVICE_URL` | order | `http://localhost:8082` | 商品服务地址 |

### 10.2 Docker 构建

每个服务统一 Dockerfile 格式：

```dockerfile
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE {port}
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 10.3 Minikube 部署要点

**部署流程：**

```
mvn clean package → docker build → minikube image load → kubectl apply
```

**K8s 资源清单：**

| 文件 | 说明 |
|------|------|
| `k8s/mysql-deployment.yaml` | MySQL 数据库 |
| `k8s/mongodb-deployment.yaml` | MongoDB 数据库 |
| `k8s/redis-deployment.yaml` | Redis 缓存 |
| `k8s/user-deployment.yaml` | 用户服务 |
| `k8s/product-deployment.yaml` | 商品服务 |
| `k8s/cart-deployment.yaml` | 购物车服务 |
| `k8s/order-deployment.yaml` | 订单服务（新增） |
| `k8s/web-deployment.yaml` | 前端 Nginx（重写为前端镜像） |

**关键配置：**

- 各服务通过 K8s Service 名称互相访问（如 `http://cart-service:8083`）
- order-service 环境变量：
  ```yaml
  env:
    - name: CART_SERVICE_URL
      value: "http://cart-service:8083"
    - name: PRODUCT_SERVICE_URL
      value: "http://product-service:8082"
    - name: MYSQL_URL
      value: "jdbc:mysql://mysql-service:3306/ecommerce"
  ```
- JWT_SECRET 等敏感配置通过 K8s Secret 注入
- 资源限制：order-service JVM 限制 `-Xmx256m`（Minikube 内存有限）
- 访问方式：`kubectl port-forward service/web-service 8080:80`

**镜像版本管理：**

每次代码修改后递增版本号（v1 → v2 → v3），避免 K8s 镜像缓存：

```bash
docker build -t xxx-service:v2 .
minikube image load xxx-service:v2
kubectl set image deployment/xxx-service xxx-service=xxx-service:v2
```

---

## 十一、API 接口总览

| 方法 | 路径 | 服务 | 鉴权 | 说明 |
|------|------|------|------|------|
| POST | `/users/register` | user-service | 否 | 用户注册 |
| POST | `/users/login` | user-service | 否 | 用户登录，返回 JWT |
| GET | `/products` | product-service | 是 | 商品列表（分页） |
| GET | `/products/{id}` | product-service | 是 | 商品详情 |
| POST | `/reviews` | product-service | 是 | 发表评价 |
| GET | `/reviews/{productId}` | product-service | 是 | 评价列表 |
| POST | `/cart/add` | cart-service | 是 | 添加商品到购物车 |
| GET | `/cart/{userId}` | cart-service | 是 | 获取购物车 |
| DELETE | `/cart/{userId}/{productId}` | cart-service | 是 | 移除购物车单品 |
| DELETE | `/cart/{userId}` | cart-service | 是 | 清空购物车 |
| POST | `/orders` | order-service | 是 | 创建订单 |
| GET | `/orders/user/{userId}` | order-service | 是 | 用户订单列表 |
| GET | `/orders/{id}` | order-service | 是 | 订单详情 |
